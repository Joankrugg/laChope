<div class="offnavs"></div>
<% other = [] %>
<% @beers.each do |b| %>
  <% if b.design_color.present? %>
    <% if b.design_color.name == 'Autre' %>
      <% other << b %>
    <% end %>
  <% else %>
    <% if b.typical_beer.design_color.name == 'Autre' %>
      <% other << b %>
    <% end %>
  <% end %>
<% end %>
<div class="container top">
  <div class="row">
    <div class="mx-auto d-none d-lg-block col-lg-3 index">
      <%= render 'shared/intra_links' %>
      <%= render 'articles/articles_side' %>
    </div>
    <div class="col-lg-9">
      <div class="card shadow">
        <h1 class='title-top'><%= title 'Bières Colorées' %></h1>
      </div>
      <div class="row">
        <% other.each do |b| %>
          <div class="mx-auto col-9 col-sm-5 col-lg-4 index">
            <%= render 'beer_card', beer: b %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
