<div class="offnavs"></div>
<div class="container top">
  <div class="row">
    <div class="col-xs-12 col-sm-12 col-lg-1"></div>
    <div class="col-xs-12 col-sm-12 col-lg-8">
      <div class="card space">
        <h1 class='page-title center'>
          STYLES DE BIÈRES
        </h1>
        <%= form_tag(typical_beers_path, method: :get) do %>
          <%= text_field_tag :search, nil, placeholder: 'Type de bière ?', class: 'search center' %>
          <%= submit_tag 'Rechercher', class: 'search' %>
        <% end %>
      </div>
      <div class="card">
        <h2 class='header-text'>Les bières classiques sont les bières représentatives d'un style ; on trouve facilement les marques citées en exemple dans le commerce ; elles participent à l'histoire et à l'évolution d'un ou de plusieurs styles de bières. À l'atelier, elle servent d'indicateur pour définir un projet et de socle pour la compréhension d'un style. Ces styles appartiennent aux familles plus larges.</h2>
      </div>
      <div class="row">
        <% @typical_beers.each do |b| %>
          <div class="mx-auto col-xs-8 col-sm-6 col-lg-6">
            <%= render 'typical_beer_card', typical_beer: b %>
          </div>
        <% end %>
      </div>
    </div>
    <div class="mx-auto col-xs-8 col-sm-12 col-lg-3">
      <%= render 'shared/social-networks' %>
      <%= render 'shared/intra_links' %>
      <div class="card sides bottom index-side">
        <h4 class="title-top">TARGET</h4>
        <% @beers.each do |b| %>
          <% if b.target? %>
            <%= link_to b do %>
              <div class="beer-link card-infos">
                <p class='beer-link-text'>Nom : <%= b.name %></p>
                <p class='beer-link-text'>Style : <%= b.style.name %></p>
                <div class="line"></div><br>
              </div>
            <% end %>
          <% end %>
        <% end %>
      </div>
      <div class="card sides top bottom">
        <h4 class="title-top">A Lire</h4>
        <% @articles.each do |a| %>
          <%= link_to a do %>
            <div class="row article-link card-infos">
              <div class="col-lg-4"><%= cl_image_tag a.photo.key, class: 'avatar-square img-fluid' %></div>
              <div class="col-lg-8">
                <p class='article-link-text'><%= a.title %></p>
                <p class='article-link-text'><%= a.theme.name %></p>
                <p class='article-link-text'><%= a.created_at.strftime("%d/%m/%Y") %> </p>
                <div class="line center"></div><br>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
