<div class="offnavs"></div>
<div class="container top">
  <div class="row">
    <div class="col-lg-1"></div>
    <div class="col-lg-8">
      <h1 class="title card center">Bocks</h1>
      <div class="card">
        Originaires du nord de l’Allemagne ces bières titrent entre 6,5 et 7° mais ne sont pas très puissantes en goût. Peu d’amertume, peu de malt, peu de corps.
      </div>
      <div class="row">
        <% b = [] %>
        <% @typical_beers.each do |tb| %>
          <% if tb.beer_family.name == 'Bock' %>
            <div class="mx-auto col-xs-8 col-sm-6 col-lg-6 index">
              <%= render 'typical_beer_card', typical_beer: tb %>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
    <div class="mx-auto col-xs-8 col-sm-12 col-lg-3 index">
      <%= render 'articles/articles_side' %>
      <%= render 'beers/ales_side' %>
      <%= render 'beers/mixed_side' %>
    </div>
  </div>
</div>
