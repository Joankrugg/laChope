<div class="offnavs"></div>
<div class="container top">
  <div class="row">
    <div class="col-lg-1"></div>
    <div class="col-lg-8">
      <h1 class="title card center">Alts</h1>
      <div class="card">
        <p class='header-text'>La Altbier est une bière de fermentation haute brassée essentiellement dans la région de Düsseldorf en Allemagne. Titrant entre 4,5 et 5 % d'alcool et de couleur cuivre à brun, elle se caractérise par un goût combinant l'amer et le sucré.</p>
      </div>
      <div class="row">
        <% alt = [] %>
        <% @typical_beers.each do |tb| %>
          <% if tb.beer_family.name == 'Alts' %>
            <div class="mx-auto col-xs-8 col-sm-6 col-lg-6 index">
              <%= render 'typical_beer_card', typical_beer: tb %>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
    <div class="mx-auto col-xs-8 col-sm-12 col-lg-3 index">
      <%= render 'articles/articles_side' %>
      <%= render 'beers/ales_side' %>
      <%= render 'beers/mixed_side' %>
    </div>
  </div>
</div>
