<div class="offnavs"></div>
<div class="container top">
  <div class="row">
    <div class="col-lg-1"></div>
    <div class="col-lg-8">
      <h1 class="title card center">Pale Ales</h1>
      <div class="card">
        <p class='header-text'>Une pale ale est une Ale : une bière d’inspiration anglaise, à fermentation haute. Brassée au malt pale, la pale ale est un type de bière qui propose généralement un goût caramélisé typique, un corps sucré et un taux d’alcool faible.</p>
      </div>
      <div class="row">
        <% pa = [] %>
        <% @typical_beers.each do |tb| %>
          <% if tb.beer_family.name == 'Pale Ales' %>
            <div class="mx-auto col-xs-8 col-sm-6 col-lg-6 index">
              <%= render 'typical_beer_card', typical_beer: tb %>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
    <div class="mx-auto col-xs-8 col-sm-12 col-lg-3 index">
      <%= render 'articles/articles_side' %>
      <%= render 'beers/ales_side' %>
      <%= render 'beers/mixed_side' %>
    </div>
  </div>
</div>
