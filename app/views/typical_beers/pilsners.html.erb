<div class="offnavs"></div>
<div class="container top">
  <div class="row">
    <div class="col-lg-1"></div>
    <div class="col-lg-8">
      <h1 class="title card center">Pilsners</h1>
      <div class="card">
        <p class='header-text'>La pilsner, du nom de la ville de Pilsen en République tchèque, est un type de bière blonde et limpide, de fermentation basse apparenté au type lager. Elle titre environ 5 degrés d'alcool et possède une amertume moyenne, dépendant du type de houblon utilisé..</p>
      </div>
      <div class="row">
        <% pil = [] %>
        <% @typical_beers.each do |tb| %>
          <% if tb.beer_family.name == 'Pilsner' %>
            <div class="mx-auto col-xs-8 col-sm-6 col-lg-6 index">
              <%= render 'typical_beer_card', typical_beer: tb %>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
    <div class="mx-auto col-xs-8 col-sm-12 col-lg-3 index">
      <%= render 'articles/articles_side' %>
      <%= render 'beers/ales_side' %>
      <%= render 'beers/mixed_side' %>
    </div>
  </div>
</div>
