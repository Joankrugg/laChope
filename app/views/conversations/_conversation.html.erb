<div class="container" id='mailbox'>
  De : <%= conversation.with(current_user).name %>
  <% if conversation.personal_messages.last.read != true %>
    <%= 'statue: non lu' %>
  <% end %>
  <br>
  <em>MSG : <%= conversation.personal_messages.last.body.truncate(50) %></em>
  <br>
  <%= link_to 'Répondre', conversation_path(conversation), class: 'btn btn-ghost' %>
  <hr>
</div>
<script type="text/javascript">

  $(document).ready(function(){
    function updateStatus() {
      $( "#mailbox" ).load(window.location.href + " #mailbox" );
    }
    setInterval(updateStatus, 4000);
  });
</script>
