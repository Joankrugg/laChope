<%= link_to edit_market_place_path(place) do %>
  <h5><%= place.name %></h5>
  <p><%= place.city %></p>
  <% if place.beers.present? %>
    <% place.beers.each do |f| %>
      <p><%= f.name %></p>
    <% end %>
  <% end %>
<% end %>
